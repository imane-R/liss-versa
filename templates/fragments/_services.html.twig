{% for service in services %}
  {% if service.Categorie.nom == 'Lissage' %}
    <li>
      <a href="{{ path('app_lissage', { slug: service.slug }) }}">{{ service.titre }}</a>
    </li>
  {% endif %}
{% endfor %}
